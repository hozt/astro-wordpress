---
/**
 * @file Content.astro
 * @description Generic HTML content renderer that processes WordPress shortcodes and icon replacements.
 */
const { htmlContent } = Astro.props;
---
<div class="main-body">
    <div class="content" set:html={htmlContent}></div>
</div>

